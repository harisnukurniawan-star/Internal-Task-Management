import { PageHeader } from "@/components/page-header";
import { createClient } from "@/lib/supabase/server";
import { getCurrentPeriod } from "@/lib/data";

export default async function LeaderboardPage(){const supabase=await createClient();const period=await getCurrentPeriod();let q=supabase.from("leaderboard_weekly").select("rank,weighted_points,avg_score,completion_rate,on_time_rate,total_assigned,total_approved,employees(full_name)").order("rank",{ascending:true});if(period)q=q.eq("period_id",period.id);const {data}=await q;const rows=data??[];return <><PageHeader title="Weekly Leaderboard" subtitle={period?.label||"Periode aktif"}/><div className="table-wrap"><table><thead><tr><th>Rank</th><th>Employee</th><th>Points</th><th>Avg Score</th><th>Completion</th><th>On-time</th><th>Approved / Assigned</th></tr></thead><tbody>{rows.map((r:any)=><tr key={`${r.rank}-${r.employees?.full_name}`}><td><strong>#{r.rank||"-"}</strong></td><td>{r.employees?.full_name}</td><td>{Number(r.weighted_points).toFixed(1)}</td><td>{Number(r.avg_score).toFixed(1)}</td><td>{Number(r.completion_rate).toFixed(0)}%</td><td>{Number(r.on_time_rate).toFixed(0)}%</td><td>{r.total_approved} / {r.total_assigned}</td></tr>)}</tbody></table></div></>}
